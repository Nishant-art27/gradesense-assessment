node_modules/
dist/
build/
*.tsbuildinfo

# Secrets — never commit real keys
.env
.env.local
*.pem

# Runtime data (grading history, uploaded bytes) — regenerated by using the app
data/
packages/server/data/
uploads/

# Note: fixtures/answers/*.pdf and docs/example-annotated-answer.pdf are
# deliberately NOT ignored. They are authored deliverables, not build output.

# OS / editor
.DS_Store
.idea/
.vscode/
*.log

# Recording / submission prep — local only
recording_sample/
recordingtb.md
tests-and-outputs/
